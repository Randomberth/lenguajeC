#include<stdio.h> // necesaria para printf
#include<math.h> // necesaria para sqrt (raiz cuadrada)


int main (){
    int i = 9;
    double r = 0;

    r = sqrt((double)i/2);
    printf("\nLa raiz cuadrada es: %g\n\n", r);

     
}